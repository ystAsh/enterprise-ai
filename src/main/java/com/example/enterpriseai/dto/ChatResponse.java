/*
 * =============================================================================
 * 클래스명 : ChatResponse
 * =============================================================================
 * 목적
 *  - Gemini가 생성한 답변을 클라이언트에게 반환한다.
 *  - 현재 단계에서는 답변만 반환한다.
 *  - 이후 대화 ID, 질문 유형, 참조 문서 등의 정보를 추가할 수 있다.
 */

package com.example.enterpriseai.dto;

public record ChatResponse(

        // Gemini가 생성한 답변
        String answer

) {
}